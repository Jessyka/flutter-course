import 'package:flutter/material.dart';

class WeekCostComponent extends StatelessWidget{

  final List<int> weekCost;

  WeekCostComponent(this.weekCost);

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }

}